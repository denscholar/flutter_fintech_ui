
import 'package:flutter/material.dart';

// Colors
const Color primaryColor = Color(0xFFF65959);
const Color secondaryColor = Color(0xFF777782);
const Color kBottomNavBarColor = Color(0xFF1D1F24);
const Color textColor = Color(0xFFFFFFFF);

// Padding
const EdgeInsets defaultPadding = EdgeInsets.all(10.0);
const EdgeInsets largePadding = EdgeInsets.all(20.0);
const EdgeInsets smallPadding = EdgeInsets.all(5.0);

// Margin
const EdgeInsets defaultMargin = EdgeInsets.all(10.0);
const EdgeInsets largeMargin = EdgeInsets.all(20.0);
const EdgeInsets smallMargin = EdgeInsets.all(5.0);